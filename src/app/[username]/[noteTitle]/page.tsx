import React from "react";

const page = () => {
  // SEARCH FOR NOTE IN USERDB
  // IF NOTE DOES NOT EXIST IN USERDB RETURN NOTE NOT FOUND
  // ELSE RETURN NOTE WITH FURTHER SELECTED TOPS TO 0
  return <div>SELECTED NOTE DOES NOT EXIST</div>;
};

export default page;
